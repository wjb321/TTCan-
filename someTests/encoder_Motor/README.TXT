定时器的形式进行编码器模拟，通过外部引脚输入两项正交的定时器中断信号，其信号是
正交的，即其相位相差90度，然后根据输入的信号计算上升下降沿的计数，从而得到4倍频的
信号，根据公式(此处用的是M方法，即在一定时间内得到编码器的数据然后进行计算，从而得到具体
的速度)

26.06.22
问题分析：
在使用频率法的时候，考虑当采样脉冲更改的时候，对应的脉冲数也会合适的变化
即：Delta T (sample impulse) = 0.01s, Delta N (encoder impulses) = 8
rps = N/(z* T) != (N - Delta N )/ (z* (T - Delta T)  ) 
这里的变化是描述在T变化后，对应N也是相同的变化，这点是恒定。

接下来要补充的就是T方法。配置高频脉冲然后根据编码一个周期的变化来进行高频信号的计数。